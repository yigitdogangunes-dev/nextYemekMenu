import LoginClient from "@/pages/LoginClient";

export const metadata = {
  title: "Giriş Yap | YEMEK",
  description: "Yemek sipariş sistemine giriş yapın.",
};

export default function LoginPage() {
  return (
    <main>
      <LoginClient />
    </main>
  );
}
